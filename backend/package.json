{
  "name": "quran-app-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts src/local.ts --target=node --outdir=dist --external=hono",
    "dev": "bun --watch src/local.ts",
    "start": "bun dist/local.js",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.7.4"
  },
  "devDependencies": {
    "@types/bun": "^1.2.2",
    "typescript": "^5.8.2"
  }
}
